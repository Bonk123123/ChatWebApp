import NavbarSkelets from '@/components/NavbarComponents/NavbarSkelets/NavbarSkelets';
import React from 'react';

const Loading = () => {
    return <NavbarSkelets />;
};

export default Loading;
